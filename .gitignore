# --- Original game (download from Internet Archive) ---
original/*.swf
/sports_heads_football_championship.swf
*.torrent
*.sqlite

# --- Build outputs (rebuild with scripts/build-60fps.bat) ---
build/*.swf
build/*.xml
build/_*/
build/_work*
build/_60fps_work.swf
build/verify-*/
build/*_check/

# Keep the build folder in git
!build/.gitkeep

# --- Custom head / SWF experiments (local only) ---
custom/

# --- Tool installers (extracted folders are kept locally) ---
tools/*.zip
tools/*.msi

# --- Release build output ---
release/staging/
release/dist/
release/ruffle-download/

# --- Ruffle binary (download locally or via release script) ---
tools/ruffle/ruffle.exe
tools/ruffle/*.dll

# --- Node (if used for build scripts) ---
node_modules/

# --- OS ---
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# --- Editor / IDE ---
*.swp
*.swo
*~
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- Logs & env ---
*.log
.env
.env.*
